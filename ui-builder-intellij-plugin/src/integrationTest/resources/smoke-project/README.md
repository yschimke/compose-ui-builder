# Installed plugin smoke project

Starter opens this directory in a real installed IDE before activating the Compose UI Builder tool
window. It intentionally contains no build: the plugin's offline editor must start without one.
