[
  {
    "field": "componentMenu.groupOrder",
    "why": "Deliberate, and the first substantive thing the contract changes rather than moves. The frozen order — Screens, Layout, Lists, Actions, Selection, Containment, Communication, Content, Embedded — is a taxonomy this repository invented for a catalog whose components it has never compiled; 'Lists', 'Content' and 'Embedded' are not words wear-m3-catalog uses about itself. The policy states that catalog's own @file:CatalogGroup(section = ...) vocabulary instead, which is the same vocabulary its published sticker sheet is already organised by, so the builder's insert panel and the catalog's own site stop disagreeing about what a component is filed under. Expect the shelves to move at cutover; that is the change, not a regression.\n\nRE-PINNED 2026-09-12, and the re-pin is this entry's mechanism working rather than failing. The value recorded here was Layout, Navigation, Actions, Selection, Text, Containment, Communication, Motion, Styles, Horologist — ten shelves the catalog had ALSO stopped using. Its own policy says so: 72 of 76 components named a group that list omitted, so the fallback sorted them after nine EMPTY shelves. wear-m3-catalog#455 replaced the list with the twenty-one groups its components actually carry, and the value below is now what BOTH sides state — the authored `ui-builder.policy.json` and the published `ui-builder.json` agree on it exactly. Because this entry pins the policy value and not just the field name, the gate reported it as an unusable exemption the moment the catalog moved instead of waving the change through, which is the whole point of pinning both sides.",
    "policy": [
      "Lists and screens",
      "Fast scrolling",
      "Cards",
      "Dialogs",
      "Buttons",
      "Icon buttons",
      "Edge-hugging buttons",
      "Toggle buttons",
      "Selection buttons",
      "Sliders and steppers",
      "Pickers",
      "Text",
      "Iconography",
      "Progress indicators",
      "Position indicators",
      "Pagers",
      "Swipe to dismiss",
      "Swipe to reveal",
      "One-handed gestures",
      "Sign-in",
      "Media controls"
    ],
    "frozen": [
      "Screens",
      "Layout",
      "Lists",
      "Actions",
      "Selection",
      "Containment",
      "Communication",
      "Content",
      "Embedded"
    ]
  },
  {
    "field": "previewSurfaces",
    "why": "Prose only, and the load-bearing halves agree exactly: native is authoritative/android and wasm is approximate on both sides. What differs is the `reason` each states, and the catalog's is the one that should survive — the frozen text was written here, about a catalog this repository has never compiled, and says 'drawn by its nearest Material 3 lookalike' where the catalog can say which of its components actually claim an adapter and which fall back to a placeholder. That is a fact only the catalog holds. Pinned rather than waived by field name: both exact values are recorded, so if either side's fidelity or backend ever moves, this entry stops applying and the difference comes back — which is the whole reason previewSurfaces is compared at all rather than carried.",
    "policy": {
      "wasm": {
        "fidelity": "approximate",
        "reason": "The builder's canvas is Compose Multiplatform for Wasm, which cannot link an Android AAR and therefore cannot call Wear Compose Material 3 at all. It draws the screen frame from the measured geometry below, and every component as a placeholder or — where this catalog claims one — through a Material 3 adapter the builder already ships. It is a drawing aid; it is not what a watch shows."
      },
      "native": {
        "fidelity": "authoritative",
        "backend": "android",
        "reason": "The generated Kotlin compiled against this module's own classpath and rendered under Robolectric: the same lane that produces this catalog's stickers."
      }
    },
    "frozen": {
      "native": {
        "backend": "android",
        "fidelity": "authoritative"
      },
      "wasm": {
        "fidelity": "approximate",
        "reason": "The canvas is Compose Multiplatform for Wasm and Wear Material 3 is an Android AAR, so every Wear component here is drawn by its nearest Material 3 lookalike. Author on it; do not read a size, a colour or a shape off it. The Android preview compiles this design's own generated Kotlin against real Wear Compose."
      }
    }
  },
  {
    "field": "platformLabel",
    "why": "The frozen catalog carries no platformLabel at all — the synthesised Wear catalog was never asked for one, and the builder derives a label from the platform word today. 'Wear' is what the catalog calls itself and what its own published sticker sheet is titled, so it is the string a person recognises. Nothing here can check it; this entry is somebody having read it.",
    "policy": "Wear",
    "frozen": null
  },
  {
    "field": "frame.adapter",
    "why": "The frozen catalog names no adapter: the server picks the Wear frame from `when (catalogSystemId)`, so the fact exists only as a branch and never as data. `frame/round-screen` is the adapter this build already ships and already draws this catalog with, which is what makes the move a transcription rather than a change. Reviewed against the renderer's own Wear branch.",
    "policy": "frame/round-screen",
    "frozen": null
  },
  {
    "field": "frame.seedDevice",
    "why": "The device a new design opens on, which the frozen catalog does not carry because the server picks the Wear frame from `when (catalogSystemId)` and never states a seed. Reviewed against two things that already agree on it rather than against the golden: `id:wearos_small_round` is the FIRST breakpoint in wear-m3-catalog's own `catalog.spec.json` (192dp), which is the documented default for this field, and it is the frame this build already opens a new Wear screen on — `UiBuilderTemplates` seeds the small-round canvas and `WearScreenCodeExporter.WEAR_PARITY_DEVICE` is the same id. So this states existing behaviour rather than choosing anything.",
    "policy": "id:wearos_small_round",
    "frozen": null
  },
  {
    "field": "frame.geometry",
    "why": "The measured contentPadding table, which the frozen catalog does not carry because the server holds these three numbers as transcribed constants in UiBuilderRenderer rather than in the catalog. They are the SAME three pairs, and the catalog's copy is the one written by a test — ScreenScaffoldContentPaddingTest composes a real ScreenScaffold at each round size and asserts the committed file equals its measurement, in both directions. So this is reviewed against a probe rather than against the golden, which is the stronger check of the two and the entire reason the block moved.",
    "policy": {
      "contentPadding": [
        {
          "screenDp": 192,
          "horizontalDp": 10,
          "verticalDp": 20
        },
        {
          "screenDp": 227,
          "horizontalDp": 12,
          "verticalDp": 23
        },
        {
          "screenDp": 240,
          "horizontalDp": 13,
          "verticalDp": 24
        }
      ]
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/button-group",
    "why": "Reshelved by wear-m3-catalog#455, and covered in principle by the `componentMenu.groupOrder` entry above: the frozen shelf name is from the taxonomy this repository invented, the policy's is the catalog's own `@file:CatalogGroup(name = …)`. Listed component by component anyway rather than waived at the `componentMenu.components` level, because a per-component entry pins BOTH values and so fails if this component is moved again or loses a field, where a blanket waiver would hide it. The record symbol is unchanged, which is what makes this a move rather than a substitution: `androidx.wear.compose.material3.ButtonGroupKt.ButtonGroup`.",
    "policy": {
      "group": "Buttons"
    },
    "frozen": {
      "group": "Actions"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/checkbox-button",
    "why": "Reshelved by wear-m3-catalog#455, and covered in principle by the `componentMenu.groupOrder` entry above: the frozen shelf name is from the taxonomy this repository invented, the policy's is the catalog's own `@file:CatalogGroup(name = …)`. Listed component by component anyway rather than waived at the `componentMenu.components` level, because a per-component entry pins BOTH values and so fails if this component is moved again or loses a field, where a blanket waiver would hide it. The record symbol is unchanged, which is what makes this a move rather than a substitution: `androidx.wear.compose.material3.CheckboxButtonKt.CheckboxButton`.",
    "policy": {
      "group": "Selection buttons"
    },
    "frozen": {
      "group": "Selection"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/icon",
    "why": "Reshelved by wear-m3-catalog#455, and covered in principle by the `componentMenu.groupOrder` entry above: the frozen shelf name is from the taxonomy this repository invented, the policy's is the catalog's own `@file:CatalogGroup(name = …)`. Listed component by component anyway rather than waived at the `componentMenu.components` level, because a per-component entry pins BOTH values and so fails if this component is moved again or loses a field, where a blanket waiver would hide it. The record symbol is unchanged, which is what makes this a move rather than a substitution: `androidx.wear.compose.material3.IconKt.Icon`. Note `Iconography` is the one name in `groupOrder` that no `@file:CatalogGroup` in `catalog/src` declares — this component comes from the library rather than from a catalog section file, which is why it carries a shelf no file names. Checked, not a typo.",
    "policy": {
      "group": "Iconography"
    },
    "frozen": {
      "group": "Content"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/list-header",
    "why": "Reshelved by wear-m3-catalog#455, and covered in principle by the `componentMenu.groupOrder` entry above: the frozen shelf name is from the taxonomy this repository invented, the policy's is the catalog's own `@file:CatalogGroup(name = …)`. Listed component by component anyway rather than waived at the `componentMenu.components` level, because a per-component entry pins BOTH values and so fails if this component is moved again or loses a field, where a blanket waiver would hide it. The record symbol is unchanged, which is what makes this a move rather than a substitution: `androidx.wear.compose.material3.ListHeaderKt.ListHeader`.",
    "policy": {
      "group": "Lists and screens"
    },
    "frozen": {
      "group": "Lists"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/list-sub-header",
    "why": "Reshelved by wear-m3-catalog#455, and covered in principle by the `componentMenu.groupOrder` entry above: the frozen shelf name is from the taxonomy this repository invented, the policy's is the catalog's own `@file:CatalogGroup(name = …)`. Listed component by component anyway rather than waived at the `componentMenu.components` level, because a per-component entry pins BOTH values and so fails if this component is moved again or loses a field, where a blanket waiver would hide it. The record symbol is unchanged, which is what makes this a move rather than a substitution: `androidx.wear.compose.material3.ListHeaderKt.ListSubHeader`.",
    "policy": {
      "group": "Text"
    },
    "frozen": {
      "group": "Lists"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/radio-button",
    "why": "Reshelved by wear-m3-catalog#455, and covered in principle by the `componentMenu.groupOrder` entry above: the frozen shelf name is from the taxonomy this repository invented, the policy's is the catalog's own `@file:CatalogGroup(name = …)`. Listed component by component anyway rather than waived at the `componentMenu.components` level, because a per-component entry pins BOTH values and so fails if this component is moved again or loses a field, where a blanket waiver would hide it. The record symbol is unchanged, which is what makes this a move rather than a substitution: `androidx.wear.compose.material3.RadioButtonKt.RadioButton`.",
    "policy": {
      "group": "Selection buttons"
    },
    "frozen": {
      "group": "Selection"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/screen-scaffold",
    "why": "Reshelved by wear-m3-catalog#455, and covered in principle by the `componentMenu.groupOrder` entry above: the frozen shelf name is from the taxonomy this repository invented, the policy's is the catalog's own `@file:CatalogGroup(name = …)`. Listed component by component anyway rather than waived at the `componentMenu.components` level, because a per-component entry pins BOTH values and so fails if this component is moved again or loses a field, where a blanket waiver would hide it. The record symbol is unchanged, which is what makes this a move rather than a substitution: `androidx.wear.compose.material3.ScreenScaffoldKt.ScreenScaffold`.",
    "policy": {
      "group": "Fast scrolling"
    },
    "frozen": {
      "group": "Screens"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/slider",
    "why": "Reshelved by wear-m3-catalog#455, and covered in principle by the `componentMenu.groupOrder` entry above: the frozen shelf name is from the taxonomy this repository invented, the policy's is the catalog's own `@file:CatalogGroup(name = …)`. Listed component by component anyway rather than waived at the `componentMenu.components` level, because a per-component entry pins BOTH values and so fails if this component is moved again or loses a field, where a blanket waiver would hide it. The record symbol is unchanged, which is what makes this a move rather than a substitution: `androidx.wear.compose.material3.SliderKt.Slider`.",
    "policy": {
      "group": "Sliders and steppers"
    },
    "frozen": {
      "group": "Selection"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/stepper",
    "why": "Reshelved by wear-m3-catalog#455, and covered in principle by the `componentMenu.groupOrder` entry above: the frozen shelf name is from the taxonomy this repository invented, the policy's is the catalog's own `@file:CatalogGroup(name = …)`. Listed component by component anyway rather than waived at the `componentMenu.components` level, because a per-component entry pins BOTH values and so fails if this component is moved again or loses a field, where a blanket waiver would hide it. The record symbol is unchanged, which is what makes this a move rather than a substitution: `androidx.wear.compose.material3.StepperKt.Stepper`.",
    "policy": {
      "group": "Sliders and steppers"
    },
    "frozen": {
      "group": "Selection"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/switch-button",
    "why": "Reshelved by wear-m3-catalog#455, and covered in principle by the `componentMenu.groupOrder` entry above: the frozen shelf name is from the taxonomy this repository invented, the policy's is the catalog's own `@file:CatalogGroup(name = …)`. Listed component by component anyway rather than waived at the `componentMenu.components` level, because a per-component entry pins BOTH values and so fails if this component is moved again or loses a field, where a blanket waiver would hide it. The record symbol is unchanged, which is what makes this a move rather than a substitution: `androidx.wear.compose.material3.SwitchButtonKt.SwitchButton`.",
    "policy": {
      "group": "Selection buttons"
    },
    "frozen": {
      "group": "Selection"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/text",
    "why": "Reshelved by wear-m3-catalog#455, and covered in principle by the `componentMenu.groupOrder` entry above: the frozen shelf name is from the taxonomy this repository invented, the policy's is the catalog's own `@file:CatalogGroup(name = …)`. Listed component by component anyway rather than waived at the `componentMenu.components` level, because a per-component entry pins BOTH values and so fails if this component is moved again or loses a field, where a blanket waiver would hide it. The record symbol is unchanged, which is what makes this a move rather than a substitution: `androidx.wear.compose.material3.TextKt.Text`.",
    "policy": {
      "group": "Text"
    },
    "frozen": {
      "group": "Content"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/button",
    "why": "Reshelved as above, AND the policy states no `variantProperty` where the frozen catalog states `variant`. That is a real loss, not a rename: the insert panel loses this component's variant control until the catalog declares `@BuilderComponent(variantProperty = …)`, which neither catalog does today — an authoring gap rather than a removal. Accepted deliberately (2026-09-12) and recorded rather than waived, so it is asserted here instead of disappearing into a green gate. The record symbol is unchanged, so the component itself is the same one: `androidx.wear.compose.material3.ButtonKt.Button`.",
    "policy": {
      "group": "Buttons"
    },
    "frozen": {
      "group": "Actions",
      "variantProperty": "variant"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/card",
    "why": "Reshelved as above, AND the policy states no `variantProperty` where the frozen catalog states `variant`. That is a real loss, not a rename: the insert panel loses this component's variant control until the catalog declares `@BuilderComponent(variantProperty = …)`, which neither catalog does today — an authoring gap rather than a removal. Accepted deliberately (2026-09-12) and recorded rather than waived, so it is asserted here instead of disappearing into a green gate. The record symbol is unchanged, so the component itself is the same one: `androidx.wear.compose.material3.CardKt.Card`.",
    "policy": {
      "group": "Cards"
    },
    "frozen": {
      "group": "Containment",
      "variantProperty": "variant"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/date-picker",
    "why": "Reshelved as above, AND the policy states no `variantProperty` where the frozen catalog states `type`. That is a real loss, not a rename: the insert panel loses this component's variant control until the catalog declares `@BuilderComponent(variantProperty = …)`, which neither catalog does today — an authoring gap rather than a removal. Accepted deliberately (2026-09-12) and recorded rather than waived, so it is asserted here instead of disappearing into a green gate. The record symbol is unchanged, so the component itself is the same one: `androidx.wear.compose.material3.DatePickerKt.DatePicker`.",
    "policy": {
      "group": "Pickers"
    },
    "frozen": {
      "group": "Selection",
      "variantProperty": "type"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/icon-button",
    "why": "Reshelved as above, AND the policy states no `variantProperty` where the frozen catalog states `variant`. That is a real loss, not a rename: the insert panel loses this component's variant control until the catalog declares `@BuilderComponent(variantProperty = …)`, which neither catalog does today — an authoring gap rather than a removal. Accepted deliberately (2026-09-12) and recorded rather than waived, so it is asserted here instead of disappearing into a green gate. The record symbol is unchanged, so the component itself is the same one: `androidx.wear.compose.material3.IconButtonKt.IconButton`.",
    "policy": {
      "group": "Icon buttons"
    },
    "frozen": {
      "group": "Actions",
      "variantProperty": "variant"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/text-button",
    "why": "Reshelved as above, AND the policy states no `variantProperty` where the frozen catalog states `variant`. That is a real loss, not a rename: the insert panel loses this component's variant control until the catalog declares `@BuilderComponent(variantProperty = …)`, which neither catalog does today — an authoring gap rather than a removal. Accepted deliberately (2026-09-12) and recorded rather than waived, so it is asserted here instead of disappearing into a green gate. The record symbol is unchanged, so the component itself is the same one: `androidx.wear.compose.material3.TextButtonKt.TextButton`. Its shelf is `Icon buttons` rather than `Buttons` because the catalog declares it in `sections/IconButtons.kt`; that is the catalog's filing, not a slip here.",
    "policy": {
      "group": "Icon buttons"
    },
    "frozen": {
      "group": "Actions",
      "variantProperty": "variant"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/time-picker",
    "why": "Reshelved as above, AND the policy states no `variantProperty` where the frozen catalog states `type`. That is a real loss, not a rename: the insert panel loses this component's variant control until the catalog declares `@BuilderComponent(variantProperty = …)`, which neither catalog does today — an authoring gap rather than a removal. Accepted deliberately (2026-09-12) and recorded rather than waived, so it is asserted here instead of disappearing into a green gate. The record symbol is unchanged, so the component itself is the same one: `androidx.wear.compose.material3.TimePickerKt.TimePicker`.",
    "policy": {
      "group": "Pickers"
    },
    "frozen": {
      "group": "Selection",
      "variantProperty": "type"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/edge-button",
    "why": "Reshelved from the frozen `Actions` to the catalog's own `Edge-hugging buttons`, AND the policy states no `variantProperty` where the frozen catalog states `size` — the same authoring gap as the six entries above, and nothing more than that as of the 2026-09-12 republish.\n\nIT WAS NOT ALWAYS THIS ORDINARY, and the history is the point. When first reviewed this entry recorded a SUBSTITUTION: `wear-m3/edge-button` resolved to `androidx.wear.compose.material3.AlertDialogDefaults.EdgeButton` — the button AlertDialog owns — and shelved on `Dialogs`, while the standalone `EdgeButton` that `EdgeButtons.kt` declares was reachable under no id at all. Two components published under one id, with the wrong one winning because AlertDialog sorts first alphabetically among the stickers that draw it.\n\nwear-m3-catalog#474 fixed that at source by pinning the record in `ui-builder.policy.json`, so the id now binds to `EdgeButtonKt.EdgeButton` and sits on its own shelf. This entry was re-pinned rather than deleted because a real difference survives the fix — the lost `size` variant control. Recording the substitution here instead of fixing the catalog would have frozen a collision as deliberate; that is what the pinned-both-sides design is for, and it is what forced the question.",
    "policy": {
      "group": "Edge-hugging buttons"
    },
    "frozen": {
      "group": "Actions",
      "variantProperty": "size"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/alert-dialog",
    "why": "Listed by the frozen catalog and NOT offered by the published menu — absent from `statusSemantics.components` entirely, so there is no record symbol to compare. Accepted as a recorded loss (2026-09-12) rather than waived. The menu does offer `wear-m3/alert-dialog-content` on the `Dialogs` shelf, so the surface is reachable through the catalog's own sticker; what is lost is the library composable as a directly insertable component.",
    "policy": null,
    "frozen": {
      "group": "Containment"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/confirmation-dialog",
    "why": "Listed by the frozen catalog and NOT offered by the published menu — absent from `statusSemantics.components` entirely, so there is no record symbol to compare. Accepted as a recorded loss (2026-09-12) rather than waived. As with alert-dialog, `wear-m3/confirmation-dialog-content` is offered on `Dialogs` (alongside the success and failure variants as separate entries), so the `variant` axis the frozen entry carried is expressed as distinct components rather than as a variant control.",
    "policy": null,
    "frozen": {
      "group": "Containment",
      "variantProperty": "variant"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/open-on-phone-dialog",
    "why": "Listed by the frozen catalog and NOT offered by the published menu — absent from `statusSemantics.components` entirely, so there is no record symbol to compare. Accepted as a recorded loss (2026-09-12) rather than waived. `wear-m3/open-on-phone-dialog-content` is offered on `Dialogs`, same substitution as the two above.",
    "policy": null,
    "frozen": {
      "group": "Containment"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/progress-indicator",
    "why": "Listed by the frozen catalog and NOT offered by the published menu — absent from `statusSemantics.components` entirely, so there is no record symbol to compare. Accepted as a recorded loss (2026-09-12) rather than waived. THE ONE CLEAN LOSS of the five: no `progress-indicator` counterpart is offered under any id. The menu carries `arc-`, `linear-`, `segmented-circular-` and `circular-progress-indicator` as separate components, which covers the drawing but not the frozen entry's `variant` axis over them. Worth revisiting if the catalog ever declares a unified indicator.",
    "policy": null,
    "frozen": {
      "group": "Communication",
      "variantProperty": "variant"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/transforming-lazy-column",
    "why": "Listed by the frozen catalog and NOT offered by the published menu — absent from `statusSemantics.components` entirely, so there is no record symbol to compare. Accepted as a recorded loss (2026-09-12) rather than waived. Not absent in practice: it is served as a declared BUILTIN (see `ui-builder.policy.json`'s `builtins`), so the builder offers it without a record entry. That is why it cannot appear in `componentMenu.components`, and why this is the least lossy of the five.",
    "policy": null,
    "frozen": {
      "group": "Lists"
    }
  },
  {
    "field": "componentMenu.components.wear-m3/alert-dialog-content",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.AlertDialogKt.AlertDialogContent` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Dialogs"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/animated-page",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.PagerScaffoldKt.AnimatedPage` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Pagers"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/animated-text",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.AnimatedTextKt.AnimatedText` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Text"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/app-card",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.CardKt.AppCard` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Cards"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/app-scaffold",
    "why": "ACCEPTED AS A KNOWN-WRONG SHELF, pinned so it cannot drift further unnoticed. `AppScaffold` is a Wear Material 3 component; `Fast scrolling` is a **Horologist**-section shelf declared by `catalog/src/androidMain/kotlin/ee/schimke/wearm3catalog/sections/FastScrolling.kt`, which does not reference `AppScaffold` anywhere. This is the shelf-collision mechanism, not intent: `@file:CatalogGroup` is file-level, so a library symbol drawn by several stickers takes the shelf of whichever sticker sorts first, and `FastScrollingList` draws this one indirectly through `ScreenSticker`. The corroboration is that the excluded `wear-m3/screen-sticker` landed on the same shelf by the same route. Same class as `circular-progress-indicator` on `Buttons` and `edge-button` bound to AlertDialog's overload, both fixed upstream in wear-m3-catalog#474 rather than recorded here. Recorded rather than fixed this time because the cutover should not wait on an upstream round-trip, and a recorded wrong shelf is visible where an unreviewed one is not. The fix belongs in the catalog; when it lands this entry should be DELETED, not amended, and the gate will say so by failing on the moved shelf.",
    "policy": {
      "group": "Fast scrolling"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/arc-progress-indicator",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.ArcProgressIndicatorKt.ArcProgressIndicator` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Progress indicators"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/child-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.ButtonKt.ChildButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Buttons"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/circular-progress-indicator",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.CircularProgressIndicatorKt.CircularProgressIndicator` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Progress indicators"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/compact-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.ButtonKt.CompactButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Buttons"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/confirm-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.AlertDialogDefaults.ConfirmButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Dialogs"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/confirmation-dialog-content",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.ConfirmationDialogKt.ConfirmationDialogContent` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Dialogs"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/decrease-icon",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.SliderDefaults.DecreaseIcon` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Sliders and steppers"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/dismiss-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.AlertDialogDefaults.DismissButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Dialogs"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/fading-expanding-label",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.FadingExpandingLabelKt.FadingExpandingLabel` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Text"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/failure-confirmation-dialog-content",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.ConfirmationDialogKt.FailureConfirmationDialogContent` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Dialogs"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/filled-icon-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.IconButtonKt.FilledIconButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Icon buttons"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/filled-tonal-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.ButtonKt.FilledTonalButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Buttons"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/filled-tonal-icon-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.IconButtonKt.FilledTonalIconButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Icon buttons"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/horizontal-page-indicator",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.PageIndicatorKt.HorizontalPageIndicator` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Position indicators"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/horizontal-pager-scaffold",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.PagerScaffoldKt.HorizontalPagerScaffold` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Pagers"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/icon-toggle-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.IconToggleButtonKt.IconToggleButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Toggle buttons"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/increase-icon",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.SliderDefaults.IncreaseIcon` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Sliders and steppers"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/level-indicator",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.LevelIndicatorKt.LevelIndicator` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Position indicators"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/linear-progress-indicator",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.LinearProgressIndicatorKt.LinearProgressIndicator` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Progress indicators"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/one-handed-gesture-click-indicator",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.onehandedgesture.OneHandedGestureClickIndicatorKt.OneHandedGestureClickIndicator` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "One-handed gestures"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/one-handed-gesture-horizontal-page-indicator",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.onehandedgesture.OneHandedGesturePageIndicatorKt.OneHandedGestureHorizontalPageIndicator` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "One-handed gestures"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/one-handed-gesture-scroll-indicator",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.onehandedgesture.OneHandedGestureScrollIndicatorKt.OneHandedGestureScrollIndicator` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "One-handed gestures"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/one-handed-gesture-vertical-page-indicator",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.onehandedgesture.OneHandedGesturePageIndicatorKt.OneHandedGestureVerticalPageIndicator` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "One-handed gestures"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/open-on-phone-dialog-content",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.OpenOnPhoneDialogKt.OpenOnPhoneDialogContent` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Dialogs"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/outlined-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.ButtonKt.OutlinedButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Buttons"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/outlined-card",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.CardKt.OutlinedCard` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Cards"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/outlined-icon-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.IconButtonKt.OutlinedIconButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Icon buttons"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/picker",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.PickerKt.Picker` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Pickers"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/picker-group",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.PickerGroupKt.PickerGroup` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Pickers"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/picker-group-item",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.PickerGroupScope.PickerGroupItem` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Pickers"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/primary-action-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.SwipeToRevealScope.PrimaryActionButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Swipe to reveal"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/scroll-indicator",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.ScrollIndicatorKt.ScrollIndicator` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Position indicators"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/secondary-action-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.SwipeToRevealScope.SecondaryActionButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Swipe to reveal"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/segmented-circular-progress-indicator",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.SegmentedCircularProgressIndicatorKt.SegmentedCircularProgressIndicator` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Progress indicators"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/split-checkbox-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.CheckboxButtonKt.SplitCheckboxButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Selection buttons"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/split-radio-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.RadioButtonKt.SplitRadioButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Selection buttons"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/split-switch-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.SwitchButtonKt.SplitSwitchButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Selection buttons"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/stepper-level-indicator",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.LevelIndicatorKt.StepperLevelIndicator` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Sliders and steppers"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/success-confirmation-dialog-content",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.ConfirmationDialogKt.SuccessConfirmationDialogContent` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Dialogs"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/swipe-to-dismiss-box",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.SwipeToDismissBoxKt.SwipeToDismissBox` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Swipe to dismiss"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/swipe-to-reveal",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.SwipeToRevealKt.SwipeToReveal` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Swipe to reveal"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/text-toggle-button",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.TextToggleButtonKt.TextToggleButton` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Toggle buttons"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/time-text",
    "why": "ACCEPTED AS A KNOWN-WRONG SHELF, pinned so it cannot drift further unnoticed. `TimeText` is a Wear Material 3 component; `Fast scrolling` is a **Horologist**-section shelf declared by `catalog/src/androidMain/kotlin/ee/schimke/wearm3catalog/sections/FastScrolling.kt`, which does not reference `TimeText` anywhere. This is the shelf-collision mechanism, not intent: `@file:CatalogGroup` is file-level, so a library symbol drawn by several stickers takes the shelf of whichever sticker sorts first, and `FastScrollingList` draws this one indirectly through `ScreenSticker`. The corroboration is that the excluded `wear-m3/screen-sticker` landed on the same shelf by the same route. Same class as `circular-progress-indicator` on `Buttons` and `edge-button` bound to AlertDialog's overload, both fixed upstream in wear-m3-catalog#474 rather than recorded here. Recorded rather than fixed this time because the cutover should not wait on an upstream round-trip, and a recorded wrong shelf is visible where an unreviewed one is not. The fix belongs in the catalog; when it lands this entry should be DELETED, not amended, and the gate will say so by failing on the moved shelf.",
    "policy": {
      "group": "Fast scrolling"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/title-card",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.CardKt.TitleCard` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Cards"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/vertical-page-indicator",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.PageIndicatorKt.VerticalPageIndicator` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Position indicators"
    },
    "frozen": null
  },
  {
    "field": "componentMenu.components.wear-m3/vertical-pager-scaffold",
    "why": "Additive, and read rather than waved through (2026-09-12). The frozen catalog has no opinion about this component at all, so nothing in this repository had checked it — that is what made it unreviewed rather than agreed. The shelf is the catalog's own `@file:CatalogGroup(name = …)` on the file declaring the sticker, not a name this repository chose. What was checked: the id, the shelf and the record symbol `androidx.wear.compose.material3.PagerScaffoldKt.VerticalPagerScaffold` describe the same component. Pinned rather than blanket-waived, so a later re-shelving fails this gate instead of passing quietly.",
    "policy": {
      "group": "Pagers"
    },
    "frozen": null
  },
  {
    "field": "code",
    "why": "Additive and read (2026-09-12). The catalog states `null` — it publishes no code-generation block — where the frozen catalog says nothing at all. Accepting `null` records that the absence was looked at and is the catalog's position, not an omission this gate failed to notice. If wear-m3 ever starts emitting a code block, this entry fails and somebody reads it.",
    "policy": null,
    "frozen": null
  },
  {
    "field": "templates",
    "why": "Additive and read (2026-09-12). The catalog states an empty list — it ships no starter templates — where the frozen catalog says nothing. Pinned as `[]` rather than waived so the first template the catalog adds fails this gate and gets reviewed, which is the moment it matters: templates are what a new design opens with.",
    "policy": [],
    "frozen": null
  }
]
